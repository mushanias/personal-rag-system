## v1
26 6.13
解决了伪装异步的bug，以及Import Side Effect的问题

最开始的时候，我发现我的路由是async def异步写法，
然后我想起来，异步函数调用的链路中，所有的涉及io的函数都应该是异步，
所以我回去检查代码，我回头看database发现了，QdrantClient是同步写法，
但是实际上我一开始不知道的，后面我查了才知道，我理所当然的觉得，
把链路上所有的QdrantClient换成异步的AsyncQdrantClient，
会出什么问题？没有任何问题，但是我的代码有问题，我的代码中在import的时候，
就已经初始化了（导入副作用），最要命的是，我是用同步写法初始化的，
如果我想要换成异步写法（AsyncQdrantClient）我必须要使用await来调用，
可是await不能再async函数外面调用，这样语法上就出现了bug，
怎么办？想一种方法，放进异步函数里面，但是问题来了，
我是不是每次都要调用这个函数初始化呢？那我为什么不把这个函数放进lifespan直接管理？
很好就这样做吧？进而引出一个新的理解，我为什么不把moonshot也放进去管理？
但是其实可选可不选，因为moonshot开销太小了。